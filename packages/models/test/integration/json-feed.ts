import "mocha" // using @types/mocha
import { expect } from "chai"

describe("JSON Feed", () => {

    it("Should fetch a valid JSON Feed")
    it("Should validate a valid JSON Feed")
    it("Should reject an invalid JSON Feed")
    it("Should provide a valid JSON Feed template")

})
